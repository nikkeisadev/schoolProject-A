using System;
using System.Linq;
//Importing basic things, such as System, but where is .Collections... I wonder.

namespace sorterCode
{
	public class UniverseStub
	{
        var logFile = File.ReadAllLines('ki.txt'); //Please place it here if it's possible.
        var logList = new List<string>(logFile); //Creating the list variable to store fresh informations.
	}
}
